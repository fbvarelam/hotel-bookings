name: Docker Compose Actions Workflow
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: lucasalt/act_base:latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: docker-compose up -f ./hotel-booking/docker-compose.test.yml -d
